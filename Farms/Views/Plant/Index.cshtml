
@{
    ViewBag.Title = "Plants";
}
<!-- MetisMenu CSS -->
<link href="/bower_components/metisMenu/dist/metisMenu.min.css" rel="stylesheet">
<!-- DataTables CSS -->
<link href="/bower_components/datatables-plugins/integration/bootstrap/3/dataTables.bootstrap.css" rel="stylesheet">
<!-- DataTables Responsive CSS -->
<link href="/bower_components/datatables-responsive/css/dataTables.responsive.css" rel="stylesheet">
<!-- Custom Fonts -->
<link href="/bower_components/font-awesome/css/font-awesome.min.css" rel="stylesheet" type="text/css">
<!-- DataTables JavaScript -->
<script src="/bower_components/datatables/media/js/jquery.dataTables.min.js"></script>
<script src="/bower_components/datatables-plugins/integration/bootstrap/3/dataTables.bootstrap.min.js"></script>
<!-- Select2 -->
<script src="/bower_components/select2/js/select2.min.js"></script>
<link href="/bower_components/select2/css/select2.min.css" rel="stylesheet" type="text/css"> 
<style>
    .select2-container {
        z-index: 2050;
    } 
</style>
<script>
    $(document).ready(function () {
        $.urlParam = function (name) {
            var results = new RegExp('[\?&]' + name + '=([^&#]*)').exec(window.location.href);
            return results[1] || 0;
        }

        $.get('http://localhost:56892/Plant/GetAllPlantsByParcelId?parcelId=' + $.urlParam('parcelId'), function (data) {
            setDTWithData(data);
            console.log(data);
        });

        $.ajax({
            type: "GET",
            url: "http://localhost:56892/Plant/GetAllPlantTypes",
            data: "",
            dataType: "json",
            contentType: 'application/json; charset=utf-8',
            success: function (data) {
                if (data != false) {
                    $.each(data, function () {
                        $("#PlantType").append("<option value='" + this.Id + "'>" + this.Name + "</option>");
                    });
                } else {
                    alert('Wops ERROR O.O" ');
                }
            },
            error: function () {
                alert('Wops ERROR O.O" ');
            }
        });

        $('#exampleModal').on('hidden.bs.modal',
            function() {
                cleanModalForm();
            });

        $('#myDT tbody').on('click', '.glyphicon-remove-sign', function () {
            var myRow = $(this).parents('tr');
            var myIndex = $Table.row(myRow).index();
            var dtData = $Table
                .row(myIndex)
                .data();

            $.ajax({
                type: "DELETE",
                url: "http://localhost:56892/Plant/RemovePlant",
                data: JSON.stringify({
                    plantId: dtData.Id
                }),
                dataType: "json",
                contentType: 'application/json; charset=utf-8',
                success: function (data) {
                    if (data) {
                        $Table.row(myRow).remove().draw();
                    } else {
                        alert('Wops ERROR O.O" ');
                    }
                },
                error: function () {
                    alert('Wops ERROR O.O2" ');
                }
            });
        });

        $('#myDT tbody').on('click', '.glyphicon-ok-sign', function () {
            var myRow = $(this).parents('tr');
            var myIndex = $Table.row(myRow).index();
            var d = $Table
                .row(myIndex)
                .data();

            $('#exampleModal').modal('show');

            $('#btnEdit').css('display', 'inline');
            $('#btnSave').css('display', 'none');

            $("#Id").text(d.Id);
            $("#Name").val(d.Name);
            $("#Description").val(d.Description);
            $("#PlantedDate").val(d.PlantedDate);
            $("#PlantType").val(d.PlantType);
            $("#hiddenValue").val(myIndex);
        });


        $('#myDT tbody').on('click', '.glyphicon-eye-open', function () {
            var tr = $(this).parents('tr');
            var index = $Table.row(tr).index();
            var rowData = $Table.row(index).data();
            window.location.replace("http://localhost:56892/Parcel?farmId=" + rowData.Id);
        });

    }); // End Ready
    function setDTWithData(getData) {
        $Table = $('#myDT').DataTable({
            "processing": true,
            "data": getData,
            "columns": [
                { "data": "Id" },
                { "data": "Name" },
                { "data": "Description" },
                { "data": "PlantedDate" },
                { "data": "IdParcel" },
                { "data": "PlantType" },
                { "defaultContent": "<span class='glyphicon glyphicon-remove-sign'></span> <span> | </span> <span class='glyphicon glyphicon-ok-sign''></span>" }
            ],
            responsive: true,
            "columnDefs": [
                {
                    "targets": [0,4],
                    "orderable": false
                }
            ]
        });
    }

    function cleanModalForm() {
        $('#btnEdit').css('display', 'none');
        $('#btnSave').css('display', 'inline');
        $("#modalForm div input[type='text']").val("");

        $("#modalForm div input[type='text']").each(function() {
            $(this).parent("div").removeClass("has-error");
        });
        $("#PlantType").val(1);
        $("#PlantedDate").val("");
        $("#Id").text("");
    }

    //Start Modal Logic

    function addMyInfo() { //Como limpiamos este codigo?
        $.ajax({
            type: "POST",
            url: "http://localhost:56892/Plant/AddPlant",
            data: JSON.stringify({
                Name: $("#Name").val(),
                Description: $("#Description").val(),
                PlantedDate: $("#PlantedDate").val(),
                IdParcel: $.urlParam('parcelId'),
                PlantType: $("#PlantType").val()
            }),
            dataType: "json",
            contentType: 'application/json; charset=utf-8',
            success: function (data) {
                if (data != false) {
                    $Table.row.add(data).draw();
                    $("#btnCancel").click();
                } else {
                    alert('Wops ERROR O.O" ');
                }
            },
            error: function () {
                alert('Wops ERROR O.O" ');
            }
        });
    }

    function editMyInfo() {
        $.ajax({
            type: "PUT",
            url: "http://localhost:56892/Plant/UpdatePlant",
            data: JSON.stringify({
                Id: $("#Id").text(),
                Name: $("#Name").val(),
                Description: $("#Description").val(),
                PlantedDate: $("#PlantedDate").val(),
                IdParcel: $.urlParam('parcelId'),
                PlantType: $("#PlantType").val()
            }),
            dataType: "json",
            contentType: 'application/json; charset=utf-8',
            success: function (data) {
                if (data != false) {
                    var index = $("#hiddenValue").val();
                    $Table.row(index).data(data).draw();
                    $("#btnCancel").click();
                } else {
                    alert('Wops ERROR O.O" ');
                }
            },
            error: function () {
                alert('Wops ERROR O.O2" ');
            }
        });
    }

</script>
<h2>Plants</h2>


<div class="row" style="padding:15px">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                DataTables Advanced Tables |
                <span class="glyphicon glyphicon-plus-sign" id="addNewRow" data-toggle="modal" data-target="#exampleModal" />

            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <table class="table table-striped table-bordered table-hover" id="myDT">
                        <thead>
                            <tr>
                                <th>
                                    <label>Id</label>
                                </th>
                                <th>
                                    <label>Name</label>
                                </th>
                                <th>
                                    <label>Description</label>
                                </th>
                                <th>
                                    <label>Planted Date</label>
                                </th>
                                <th>
                                    <label>Parcel</label>
                                </th>
                                <th>
                                    <label>Plant Type</label>
                                </th>
                                <th>
                                    <label>Buttons</label>
                                </th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>

<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="exampleModalLabel">Add new Row to Table</h4>
            </div>
            <div id="rowModalBody" class="modal-body">
                <form id="modalForm">
                    <div class="form-group">
                        <label for="recipient-name" class="control-label">Id:</label>
                        <label id="Id"></label>
                    </div>
                    <div class="form-group">
                        <label for="recipient-name" class="control-label">Name:</label>
                        <input type="text" class="form-control" id="Name">
                    </div>
                    <div class="form-group">
                        <label for="message-text" class="control-label">Description:</label>
                        <input type="text" class="form-control" id="Description">
                    </div>
                    <div class="form-group">
                        <label for="message-text" class="control-label">PlantedDate:</label>
                        <input type="date" class="form-control" id="PlantedDate">
                    </div>
                    <div class="form-group">
                        <label for="message-text" class="control-label">PlantType:</label>
                        <select id="PlantType" class="form-control"></select>
                    </div>
                    <input type="text" class="form-control" style="display:none" id="hiddenValue">
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" id="btnCancel" class="btn btn-default" data-dismiss="modal">Close</button>
                <button type="button" id="btnSave" class="btn btn-primary" onclick="addMyInfo()">Add Row</button>
                <button type="button" id="btnEdit" class="btn btn-primary" onclick="editMyInfo()" style="display:none">Edit</button>
            </div>
        </div>
    </div>
</div>